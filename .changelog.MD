

## [2.1.2] - AI Voice Mute Toggle
### Added
- **AI Voice Mute (M Key):** Added a state toggle (`aiVoiceMuted`) allowing players to disable ElevenLabs MP3 playbacks. When muted, the engine seamlessly routes to the ARIA fallback logic, maintaining game pacing without the voice lines.
- **UI Indicator:** The controls bar at the top right now displays the AI Voice status (On/Muted) alongside the Speech Rate.
### Changed
- **Cleanup:** Removed a legacy global 'M' key listener that was used for testing the synthesizer.
- Incremented system to v2.1.2.

## [2.1.1] - Placement Polish & AI Timing
### Added
- **Placement Hotkeys:** Enabled standard category hotkeys (1-6, T, F, H, S, L, Y, A, B) during Placement Mode. This allows experienced players to instantly jump to specific available categories after their 3rd roll without needing to cycle with Arrow Keys.
### Changed
- **Bot Reaction Timing:** Adjusted the AI's Phase 3 logic. The engine now visually registers and announces the AI's selection first, pauses, plays the `bot_score` MP3 as a reaction, and then officially saves the score to trigger the human player's turn. 
- Incremented system to v2.1.1.

## [2.1.0] - The Voice Engine
### Added
- **AI Voice Callbacks:** Ported the asynchronous audio callback engine from Keyboard Commander (`playBotAudio`) to allow AI opponents to play MP3 voice lines natively.
- **Audio Dictionary:** Implemented `window.BOT_AUDIO` to map asset keys to file paths for easy expansion.
- **File Versioning:** Introduced file-specific version comment headers (e.g., `/* core.js - v2.1.0 */`) to track modular updates.
### Changed
- **Bot Intro:** Added a voice hook when Bumbling Bot is added to the game during setup.
- **Bot Thinking:** Replaced the screen reader's Phase 1 "thinking" announcement with the `bot_think.mp3` line.
- **Bot Scoring:** Replaced the screen reader's Phase 3 "selects category" announcement with the `bot_score.mp3` line.
- Incremented system to v2.1.0.

## [2.0.0] - The Modular Migration
### Changed
- **Architecture Refactor:** Transitioned from a single-file monolith to a modular directory structure to support external audio assets and scalable AI logic.
- **Audio Extraction:** Moved all Web Audio API synthesis functions (`playTone`, `playSequence`, `playGameSound`, etc.) into `audio.js`.
- **Core Engine Extraction:** Moved game state (`window.YAHTZEE_STATE`), score calculation (`calculateScore`), and AI logic (`handleAITurn`) into `core.js`.
- **UI & Input Isolation:** `index.html` now strictly handles DOM rendering, ARIA announcements, and the global keyboard event listener.
- Incremented version to v2.0.0.

## [1.5.1] - Pacing & ARIA Sync
### Added
- **AI Pacing Control:** Added a variable speech rate toggle to control the delay of the AI's actions, preventing screen reader audio queuing.
- **Pacing Hotkey:** Pressing 'P' cycles the AI speed between Fast (3s), Medium (4.5s), and Slow (6s).
- **Visual Pacing Indicator:** Added a visual indicator in the top right of the game container for sighted users to track the current pacing speed.
### Changed
- **Codebase Cleanup:** Removed the duplicate `window.handleAITurn` function that was accidentally pasted in v1.5.0.
- Incremented version to v1.5.1.## [1.5.0] - The AI Automation Loop
### Added
- **AI Player Support:** Pressing 'B' during setup adds a computer-controlled opponent ("Bumbling Bot [AI]").
- **Asynchronous AI Engine:** Added `window.handleAITurn()` to automate dice rolling, holding, and category selection with 1.5-second delays for screen reader accessibility.
- **Grinder Personality (V1):** Bumbling Bot is programmed to aggressively target Upper Section categories and will attempt to hold matching dice to build 3-of-a-kinds and 4-of-a-kinds for the upper board.
### Changed
- Incremented version to v1.5.0.

## [1.4.9] - Streamlined Toggle Verbosity
### Changed
- **Toggle Announcements:** Reduced the verbosity when holding/releasing dice. The screen reader now only announces the die value and its new status (e.g., "6, Held") instead of repeating the die index.
- **F5 Recovery:** Formally integrated the F5 refresh bypass into the core logic to ensure browser stability.
- Incremented version to v1.4.9.

## [1.4.8] - UX Polish & Browser Compatibility
### Added
- **Browser Refresh Enabled:** Released the `F5` key from the global `preventDefault` lock, allowing users to refresh the page normally.
### Changed
- **Initial Interaction:** Updated the startup button text to "Press Enter to Begin" for better screen reader instruction.
- **Application Identity:** Changed the `aria-label` of the game container to "Application: YAY! Accessible Yahtzee" to match the official branding.
- Incremented version to v1.4.8.

## [1.4.6] - Yahtzee Bonus Logic & Simulator
### Added
- **End-Game Simulator (`Ctrl + Shift + 3`):** Dev shortcut instantly fills 12 categories for all players. Player 1 gets a "Perfect Minimum" (63 upper, all lower scored), while other players receive exactly 50% of Player 1's score. The turn is passed to Player 1 for round 13 with only Chance remaining.
- **Bonus "Free Action" Logic:** Scoring in the `Yahtzee Bonus` category no longer resets the dice or passes the turn. It acts as a free action, allowing players to log their 100 points and then select a standard category to fulfill the Joker Rule.
### Changed
- Nav Mode: Allowed `Enter` to be pressed on the `Yahtzee Bonus` category even if it already has a score, allowing Manual players to bump it from 100 to 200, etc.
- Score Mode: Initial `tempScore` now inherits the category's current value instead of resetting to 0, ensuring consecutive bonuses add up correctly.
- Incremented version to 1.4.6.

## [1.4.5] - Adaptive Turn Announcements
### Added
- Added `window.getTurnAnnouncement()` to dynamically calculate and return the shortest category list at the start of a turn.
- The game now compares empty vs. filled categories. If filled is shorter, it announces filled categories. If empty is shorter (or equal), it announces empty categories.
- Added the player's current grand total to the start-of-turn announcement.
- Appended a mode-specific action prompt to the end of the announcement ("Press D to roll dice" vs "Press Enter to set score").
### Changed
- Replaced the static turn transition strings in `setup`, `placement`, and `score` modes with the new dynamic announcement.
- Incremented version to v1.4.5.

## [1.4.4] - End Game Detection & Auto-Save
### Added
- Added `window.checkGameOver()` to automatically detect when all 13 categories are filled for all players.
- The game now automatically calculates final totals, announces the winner, and saves the records to the History and Leaderboard.
- Added a `gameOver` flag to the game state. When true, it safely disables the Roll, Hold, Enter, and Undo keys, but allows players to use arrow keys to review the final board.
### Changed
- Refactored the 'Q' reset logic to prevent duplicate leaderboard saves if the game ended naturally.
- Incremented version to v1.4.4.

## [1.4.3] - Auto-Turn Progression & Leaderboard Fix
### Added
- **Auto-Turn Progression:** Locking in a score (in both Digital and Manual modes) now automatically advances `currentPlayerIndex` to the next player.
- **Smart Cursor:** When a turn passes, the cursor automatically parks on the new player's first available empty category.
### Fixed
- Fixed the History and Leaderboard objects to save and announce the player's `name` alongside their score and date.
- The `Q` Hard Reset now loops through *all* active players and saves all of their scores to the history before wiping the board.
### Changed
- Incremented version to v1.4.3.

## [1.4.2] - The Auto-Funnel & Audio Polish
### Fixed
- Pressing `Q` (and confirming with `Y`) now completely clears the player list, wipes the board, and returns the game to the initial naming setup phase.
### Added
- Added the "Auto-Funnel" to `window.rollDice`: Taking your 3rd roll automatically shifts the game into `placement` mode and calculates your first empty category without requiring an `Enter` press.
### Changed
- Streamlined the `I` (Inspect) hotkey announcement. It now quickly reads the raw dice array (e.g., "Dice: 6, 4, held 3, 3, held 5") instead of verbose, individual die descriptions.
- Incremented version to v1.4.2.

## [1.4.1] - The "Two Roads" Engine Update
### Added
- Added a `gameMode` toggle to state (`digital` or `manual`).
- Added a new `setup_dice_choice` phase right after player creation to choose the game mode.
- Added `placement` mode for Digital players: arrow keys filter empty categories and calculate score previews automatically.
### Changed
- Preserved the legacy `score` mode for Manual players. Pressing Enter branches dynamically based on the chosen `gameMode`.
- Disabled the `D` (Roll) and `Z, X, C, V, B` (Hold) keys when playing in Manual mode to prevent accidental audio clutter.
- Incremented version to v1.4.1.

## [1.4.0] - The Auto-Scoring Math Engine (Phase 1)
### Added
- Created the `window.calculateScore(dice, categoryKey)` function to act as the game's internal rules engine.
- Programmed standard Yahtzee validation logic for all Upper and Lower section categories.
- Implemented pattern recognition for Straights (checking for sequential runs of 4 or 5) and Full Houses (checking for 3/2 splits or 5-of-a-kind).
### Changed
- Incremented version strings to v1.4.0 to mark the beginning of the Auto-Scoring transition.

## [1.3.9.1] - ARIA Robustness Update
### Fixed
- Fixed the "silent Help Menu" bug where the `?` text was injected but ignored by the screen reader.
- Refactored `window.announce` with a `setTimeout` clear/inject cycle to force screen readers to recognize new text updates.
### Changed
- Added `aria-atomic="true"` to the `#aria-announce` live region to ensure long strings are read entirely.
- Incremented version strings to v1.3.9.1.

## [1.3.9] - The Help Menu
### Added
- Implemented a comprehensive Help Menu triggered by the `?` key.
- Added detailed audio instructions covering navigation, scoring (including the Shift-modifier), dice controls, category shortcuts, and game tools.
### Changed
- Incremented version strings to v1.3.9.

## [1.3.8.1] - Ergonomic Alignment & Turn Reset
### Fixed
- Corrected the bottom-row dice toggles to follow the physical Z, X, C, V, B layout.
- Removed the 'KeyN' toggle to prevent accidental dice holds during navigation.
- Fixed a logic error in 'score' mode where rolls were not resetting to 3 after saving.
### Added
- Integrated the "Dice Shake" sound placeholder to the 'D' (Roll) key.

## [1.3.8] - Setup Guidance & Focus Fix
### Fixed
- Fixed the "Initialization Silence" bug by explicitly calling the setup announcement in `window.initGame`.
- Corrected the `aria-label` on the game container to better reflect the current `inputMode`.
### Added
- Added clear verbal instructions for the Naming Phase: "Choose the name of Player 1. Use up and down arrows, then Enter to Choose".
- Integrated "YAY!" branding into the startup sequence.

## [1.3.7.1] - Ergonomic Control Re-mapping
### Changed
- Re-mapped "Roll Dice" to the **D** key for center-finger access.
- Moved Dice Toggle keys to the bottom row (**Z, X, C, V, B**) to free up home row hotkeys.
- Restored **S** (Small Straight) and **F** (Four of a Kind) hotkeys.
- Re-mapped **Chance** to **A** and the **Remaining Audit** to **G**.
### Fixed
- Resolved key collision where dice controls were overwriting scoring hotkeys.

## [1.3.7] - Low Vision UI & Dice Foundation
### Added
- Increased global font size to 24pt (32px) for high readability.
- Expanded game container to 100% width (max 1200px) for 6-player spacing.
- Created a "Dice Stage" below the scorecard for visual dice tracking.
- Implemented vertical shifting (20px) for dice to visually distinguish "Held" vs "Rolling".

## [1.3.6.1] - Auditory Landmarks & Pacing
### Added
- Enhanced the **X** (Audit) hotkey with structural landmarks: "Upper Section" and "Lower Section" announcements.
- Added improved pacing to the audit list using punctuation to force screen reader breath pauses.
### Changed
- Refactored the audit logic to group open categories by section for better cognitive organization.
- Incremented version to 1.3.6.1 across all title and initialization strings.

## [1.3.6] - The "Open Category" Audit
### Added
- Added **X** hotkey in Navigation Mode to audit the active player's scorecard.
- Implemented an automated scan that identifies all categories with a `null` value.
- Added dynamic ARIA announcement: "Remaining for [Name]: [List of Categories]."
### Changed
- Incremented version to 1.3.6 across all title and initialization strings.

Changelog[1.3.5.2] - Structural & Setup Repair

Fixed: Moved NAME_LIBRARY into the main script block.

Fixed: Relocated setup keyboard logic from initGame to the top of the keydown listener.

Added: Proper "Start or Add" flow with ARIA announcements for dynamic player counts.

Cleanup: Removed all code leaking outside of proper HTML tags.

## [1.3.5.2] - The "Structure & Setup" Repair
### Fixed
- Structural Integrity: Removed all code dangling outside the </html> tag and consolidated script blocks.
- Hydration Safety: Updated initGame to handle empty player arrays gracefully, preventing crashes on fresh starts.
- Keydown Priority: Moved Setup Mode logic to the top of the listener so it overrides Navigation Mode.
### Added
- Confirm-to-Start Flow: After choosing a name, the user is prompted to add another or start with 'S'.
- Default Tagging: Added " (Default)" announcement to the first name for better orientation.

## [1.3.5.1] - Dynamic Player Setup & Keyfix
### Fixed
- Fixed `keydown` listener to correctly trap Up/Down arrows during the `setup` mode.
- Corrected `inputMode` priority to ensure setup keys are handled before navigation logic.
### Added
- Implemented "Confirm or Add" flow: After choosing a name, the user is prompted to press **Enter** to add another player or **S** to start the game.
- Added " (Default)" suffix to the first name in the library for Player 1.
- Updated ARIA announcements to guide the user through the dynamic setup process.

## [1.3.5] - Player Naming Phase
### Added
- Created a new `inputMode: 'setup'` to handle player name selection at game start.
- Added a library of 20 selectable names (alphabetized) for personalization.
- Implemented **Up/Down Arrows** to cycle names and **Shift + Up/Down Arrows** to jump 5 names at a time.
- Integrated chosen names into the state, table headers, and all ARIA announcements.
### Changed
- Refactored `renderScorecard` to display chosen names instead of "Player X".
- Updated `window.initGame` to trigger the setup phase before moving to the main board.

## [1.3.4.1] - Multiplayer Layout & Persistence Hotfix
### Fixed
- Updated hydration logic to catch player-count mismatches (forcing a refresh if not 6 players).
- Refactored the section divider to use a dynamic `colspan` based on the active player count.
- Increased CSS `max-width` to 900px to accommodate 6 columns.

## [1.3.4.1] - Multiplayer Layout & State Fix
### Fixed
- Updated the hydration logic to verify that the saved player count matches the current engine (6 players). This forces the board to refresh to the new layout if a mismatch is detected.
- Fixed a hardcoded `colspan` in the section divider that was breaking the table layout for more than 2 players.
- Increased the `#game-container` max-width to 900px to accommodate 6 player columns without squishing.

## [1.3.4] - Six Player Expansion
### Added
- Expanded the maximum player capacity from 2 to 6 players.
### Changed
- Updated `window.YAHTZEE_STATE` to initialize with 6 independent player objects.
- Refactored the `renderScorecard` table header to dynamically generate `<th>` tags based on the number of active players, supporting seamless future scaling.

## [1.3.3] - The Undo Feature
### Added
- Implemented a single-action 'Undo' system via the `U` hotkey in Navigation mode.
- Added `lastMove` tracking to `window.YAHTZEE_STATE` to record the most recent player and category scored.
- Added a descending `undo` sound sequence to `window.playGameSound` for distinct auditory feedback.
### Changed
- Committing a score in Score Mode now logs the action to `lastMove`.
- Executing an Undo restores the targeted category to `null`, clears the `lastMove` memory, dynamically shifts player focus back to the undone box, and recalculates the board.

## [1.3.2.1] - Legacy Save File Hotfix
### Fixed
- Fixed a fatal crash caused by loading outdated single-player `localStorage` data into the new multiplayer engine. 
- Added a schema validation check to `initGame` to gracefully clear old save data and prevent lockups.

## [1.3.2] - Multiplayer Category Locking
### Fixed
- Re-implemented Category Locking logic that was dropped during the multiplayer refactor.
- Pressing Enter on an already scored/scratched category now plays the `limit` thud and prevents entry into Score Mode.

## [1.3.1] - Multiplayer Architecture & Column Navigation
### Added
- Refactored `window.YAHTZEE_STATE` to support multiple players via a `players` array.
- Added Left/Right arrow navigation to switch focus between Player 1 and Player 2 columns.
- Added a column-switch announcement so the screen reader explicitly says "Player [X]" when moving horizontally.
- Added the `R` hotkey to instantly announce the current player's Grand Total from anywhere on the board.
### Changed
- Updated `renderScorecard` math engine to calculate totals independently for each player.
- Updated the visual HTML table to render multiple player columns side-by-side.

## [1.3.0] - Audio Feedback Completion & Leaderboard Update
### Added
- Added 'nav' (blip) and 'limit' (thud) audio feedback when dialing scores up and down within a category.
- Added 'select' (chime) audio feedback when opening the Leaderboard.
- Added 'cancel' (descending tone) audio feedback when pressing 'N' to cancel a board reset.
### Changed
- Changed the Leaderboard hotkey from `0` to `o` to prevent conflicts with number-row navigation.

## [1.2.9.1] - Navigation Scratch Announcements
### Changed
- Refined navigation announcements (`inputMode === 'nav'`) to explicitly read "Scratched" instead of "Score 0" when moving over a sacrificed category.
- Preserved the "Empty" announcement for categories that have not yet been played.

## [1.2.9] - Explicit Scratch Logic
### Added
- Added explicit "Scratch" audio announcements for the screen reader when a category's value is set to 0.
### Changed
- Navigating to `0` in Score Mode now announces "Scratch, 0" instead of just "0".
- Saving a `0` now explicitly announces that the category has been "Scratched" rather than "0 points saved."
- Entering Score Mode on an empty category defaults to "Scratch, 0".

## [1.2.8] - URL Optimization & Deployment Polish
### Changed
- Renamed `yay.html` to `index.html` to support root-level URL loading.
- Updated internal versioning to v1.2.8.
### Fixed
- Verified all audio triggers are properly linked in the newly renamed file.

## [1.2.7] - Full Interaction Audio Wiring
### Added
- Wired 'select' chime to the first Enter press (Enter Score Mode).
- Wired 'save' chord to the second Enter press (Commit Score).
- Wired 'cancel' thud to the Escape key.
### Changed
- Refined initGame sequence for immediate audio feedback upon "Start."## [1.2.5b] - Audio Volume & Initialization Fix
### Added
- Successfully wired 'nav' (880Hz blip) to Arrow keys.
- Successfully wired 'select' (ascending chime) and 'save' (C Major chord) to Enter key.
- Added 'cancel' (descending tone) to the Escape key.
### Fixed
- Boosted global gain for all game sounds to 0.4 - 0.5 (approx 40-50% volume).
- Adjusted `initGame` timing to ensure the startup sequence fires reliably across all browsers.

## [1.2.5b] - Audio Volume Boost & Response Optimization
### Added
- Created a 'cancel' sound (low-pitched descending tone) for the Escape key.
### Changed
- Increased Global Gain: Boosted base volume levels in `playGameSound` definitions.
- Instant Feedback: Removed the 1500ms delay on the 'startup' chime so it fires the moment the game starts.
- Wired 'select' and 'save' sounds to the Enter key logic.

## [1.2.5a] - Navigation Audio Integration
### Added
- Created the 'nav' sound case in the `playGameSound` function.
- Wired `window.playGameSound('nav')` to the ArrowUp and ArrowDown keys.
### Changed
- Moved `initAudio()` to the very top of the `keydown` listener to ensure the engine wakes up on the first press.## [1.2.4] - Audio Initialization Hotfix
### Fixed
- Forced the placement of the `initAudio();` wake-up call into the `initGame()` function and the `keydown` event listener to ensure the Web Audio API unlocks on user interaction.

## [1.2.0] - Web Audio API Integration (Engine)
### Added
- Integrated native HTML5 `AudioContext` synthesizer (no external MP3s).
- Added core synthesis functions: `playTone`, `playEcho`, `playSequence`, `playSweep`, and `playChord`.
- Created central `playGameSound(action)` router to manage game audio.
- Added a "Startup" chime to the `initGame()` function to verify engine activation.

## [1.1.2] - Advanced Timestamp Formatting
### Changed
- Upgraded the `date` property generation in the Reset (`Q`) logic to capture a highly detailed string (e.g., "10:04 PM, Thursday, March 14, 2026").
- Updated the Leaderboard (`0`) string format to read: "Number [X], [Score] points, [Date/Time]".
- Updated the History (`E`) string format to read: "[Score] points, [Date/Time]".
- Updated the `Ctrl + Shift + 0` developer dummy score to use the new detailed format.

## [1.1.1] - Hotkey Logic Fix
### Fixed
- Fixed a bug where Developer Shortcuts (`Ctrl + Shift + Number`) were failing. Switched the event listener to evaluate `e.code` (e.g., 'Digit1') instead of `e.key` ('1'), which previously failed because the Shift modifier transformed the key value into punctuation ('!').
### Added
- Restored the `Ctrl + Shift + 0` shortcut to inject a 500-point dummy score into the leaderboard for displacement testing.

## [1.1.0] - Persistence, History & Reset Logic
### Added
- **LocalStorage Hydration**: The game now automatically saves your state after every score and reloads it on page refresh.
- **Game History (E)**: Pressing 'E' in Navigation Mode announces your last 5 completed game scores.
- **Top 10 Leaderboard (0)**: Pressing '0' announces your all-time top 10 high scores.
- **Reset Workflow (Q)**: Pressing 'Q' triggers a confirmation mode ('Y' to save and wipe, 'N' to cancel).
- **Dev Shortcuts**: `Ctrl + Shift + 1` to instantly max the Upper Section and `Ctrl + Shift + 9` to factory reset the app.
## [1.1.0] - Persistence, History & Reset Logic
### Added
- **LocalStorage Hydration**: The game now automatically saves your state after every score and reloads it on page refresh.
- **Game History (E)**: Pressing 'E' in Navigation Mode announces your last 5 completed game scores.
- **Top 10 Leaderboard (0)**: Pressing '0' announces your all-time top 10 high scores.
- **Reset Workflow (Q)**: Pressing 'Q' triggers a confirmation mode ('Y' to save and wipe, 'N' to cancel).
- **Dev Shortcuts**: `Ctrl + Shift + 1` to instantly max the Upper Section and `Ctrl + Shift + 9` to factory reset the app.


## [1.0.5] - Layout Refinement & Navigation Shortcuts
### Changed
- Relocated "Upper Subtotal" and "Upper Bonus" rows to appear immediately after "Sixes" to act as a section separator.
- Restructured the internal navigation array to reflect the new visual order.

### Added
- Page Up/Page Down Shortcuts: 
    - Pressing **Page Down** now cycles specifically through the major totals (Upper Subtotal -> Lower Total -> Grand Total).
    - Pressing **Page Up** cycles through them in reverse.
- Immediate ARIA announcements for these new jump-navigation shortcuts.

## [1.0.4] - Totals Navigation & Bonus Logic
### Added
- Navigation support for Total rows: You can now arrow down past "Chance" to hear Upper Subtotal, Upper Bonus, Lower Total, and Grand Total.
- Yahtzee Bonus Lock: The "Yahtzee Bonus" is now skipped during navigation unless the main Yahtzee score is exactly 50.
- Logic to prevent "scoring" a total: Pressing Enter on a Total row will not enter Score Mode.

## [1.0.3] - Navigation Hotkeys
### Added
- Direct jump hotkeys for Upper Section: Keys `1` through `6`.
- Mnemonic hotkeys for Lower Section: `T` (3-of-a-kind), `F` (4-of-a-kind), `H` (Full House), `S` (Small Straight), `L` (Large Straight), `Y` (Yahtzee), and `C` (Chance).
- Logic to prevent hotkey navigation while in Score Mode to avoid accidental jumps during data entry.
- Immediate ARIA announcements upon hotkey jump to confirm the new category and its status.

## [1.0.2] - Scoring Engine & Math
### Added
- `Enter` key logic to toggle between Navigation Mode and Score Mode.
- Score Mode mechanics: Up/Down arrows to adjust score based on category `step` or `fixed` values.
- Score Mode "Chunking": `Shift + Up/Down` to jump by 5x the standard step for faster entry.
- Dynamic math engine: Real-time calculation of Upper Subtotal, Upper Bonus (35 points if subtotal >= 63), Lower Total, and Grand Total.
- Visual Totals: Appended summary rows to the bottom of the scorecard table.

## [1.0.1] - Visuals & Navigation
### Added
- `renderScorecard()` function to visually display the `window.YAHTZEE_STATE` as a clean HTML table inside `#game-container`.
- Global `keydown` listener attached to `window`.
- Arrow Up / Arrow Down navigation logic to move `s.currentCategory` sequentially through the categories.
- ARIA announcements for cursor movement (e.g., "Aces. Empty.").

## [1.0.0] - Foundation Build
### Added
- Initialized single-file architecture (`yay.html`).
- High-contrast, dark-mode CSS skeleton for sighted players.
- `<button id="initBtn" autofocus>Start Yahtzee</button>` to handle safe initialization and bypass browser auto-focus security.
- Global focus trap locked to `#game-container` that only activates after the start button is pressed.
- `window.YAHTZEE_STATE` strict data object.
- `window.announce()` ARIA-live routing function.